<p id="notice"><%= notice %></p>
<h1>Trouver un service :</h1>
<%= search_form_for @q do |f| %>
    <form class="form-inline">
      <div class="form-group">
        <%= f.label :place_cont %>
        <%= f.search_field :place_cont %>
      </div>
          
      <div class="form-group">
        <%= f.label :hour_beginning_service_eq %>
        <%= f.search_field :hour_beginning_service_eq %>
      </div>
          
      <div class="form-group">
        <%= f.label :time_spent_cont %>
        <%= f.search_field :time_spent_cont %>
      </div>
      
      <div class="form-group">
        <%= f.label :description_cont %>
        <%= f.search_field :description_cont %>
      </div>
      
      <div class="form-group">
        <%= f.label :service_type %>
        <%= f.collection_select :service_type_id, ServiceType.all, :id, :name %>
      </div>
      
      <div class="form-group">
        <%= f.label :participants_number_eq %>
        <%= f.search_field :participants_number_eq %>
      </div>
      
      <div class="form-group">
        <%= f.label :date_service_cont %>
        <%= f.search_field :date_service_cont %>
      </div>
      
      <div class="form-group">
        <%= f.label :name_cont %>
        <%= f.search_field :name_cont %>
      </div>
  </div>

  <%= f.submit %>

<%- if @services.size > 0 %>

<% end %>
  <ul class="list-group">
    <% @services.each do |service| %>
      <li class="list-group-item">
        <p><%= service.name %></p>
        <p><%= service.description %></p>
        <p><%= service.participants_name %></p>
        <p><%= service.time_spent %></p>
        <p><%= service.description %></p>
      </li>
    <% end%>
  </ul>
<% end%>
